# OS
.DS_Store

# Dependencies / build output (uncomment as the stack takes shape)
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/

# Env / secrets
.env
.env.*

# Claude / dev-environment files — local only, never tracked or pushed
CLAUDE.md
CLAUDE.local.md
.claude/
HANDOFF.md
BUILD-PROMPT.md
graphify-out/

# agent worktrees + shared build venv
.worktrees/
